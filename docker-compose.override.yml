version: "3"

services:
    runestone:
        volumes:
            - ../RunestoneComponents:/srv/RunestoneComponents
            - ./docker/sslData:/sslData

    db:
      volumes:
        - ../database/activeData:/var/lib/postgresql/data
